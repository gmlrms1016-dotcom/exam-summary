# 강의소개 / C 개요

> 자료: C-Lect01-강의소개.pdf · 실습-w01-C 설치.pdf · 교재.pdf

## 1. 강의 안내
- ⭐ **강조** — 교재: Perfect C(3판), 강환수·강환일·이동규, 인피니티북스, 2021.8
- ⭐ **강조** — 평가: 출석 20% + 중간 40% + 기말 40% (과제·실습은 가산점), 결석 4회 F
- ✍️ **필기** — 프로그램: Visual Studio C++ Community 2022 — 사용자 계정은 반드시 영문

## 2. C 언어 특징 ★암기
- ⭐ **강조** — 절차지향 언어(procedural) — 함수 중심의 구조적 프로그래밍
- 간결하고 효율적, 다양한 시스템 라이브러리 제공
- 이식성이 좋음 — 다양한 CPU·플랫폼의 컴파일러 지원

## 3. C 프로그램 실행 과정 ★핵심
- ⭐ **강조** — ① 소스코드 작성(.c) → ② 컴파일(→ 오브젝트 .obj/.o) → ③ 링킹(→ 실행파일 .exe/a.out) → ④ 실행
```c
// hello.c
#include <stdio.h>
int main(void) {
    printf("hello world!");  // 출력
    return 0;               // 프로그램 종료
}
```
### 첫 프로그램 (hello.c) — 한 줄씩 주석
```c
// hello.c — ① 소스코드 작성 (.c 파일)
#include <stdio.h>            // 전처리기: printf 가 들어 있는 stdio.h 를 가져온다
int main(void) {              // 프로그램은 main() 에서 자동으로 시작
    printf("hello world!");   // 화면에 출력 (\n 이 없어서 줄은 안 바뀜)
    return 0;                 // 0 을 돌려주며 프로그램 정상 종료
}
// ② 컴파일 Ctrl+Shift+B → hello.obj   ③ 링크 → hello.exe   ④ 실행 Ctrl+F5
```
```output
hello world!
```

- VS 단축키: 모두 저장 Ctrl+Shift+S / 빌드 [빌드]>솔루션빌드 / 실행 Ctrl+F5
- ✍️ **필기** — 주의: 파일명 입력 시 확장자 .c 를 생략하면 .cpp로 저장됨
