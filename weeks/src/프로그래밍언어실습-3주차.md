# 변수·상수·자료형 (ch03)

> 자료: C-Lect03-ch03-변수상수-stu-pdf.pdf · 수업 필기 · gpt대화내용.txt · workC 실습 코드(w03 · w03_quiz01 · w03_extra)

> 🎯 **시험 포인트** — 강의자료 **23p** [실습 1-05] sizeof 실행결과 쓰기 · **27p** [quiz 1-01] 빈칸채우기(printf 서식) + 오류 수정 · 수업 중 "시험문제!" sizeof 서식 고치기 (→ 8장)

## 1. 변수와 상수 ★핵심
- ⭐ **강조** — 변수(variable): 프로그램이 실행되는 동안 값이 바뀌는 정보, 데이터를 저장·처리하는 **기억장소**
- ⭐ **강조** — 상수(constant): 한 번 정해지면 프로그램이 끝날 때까지 바뀌지 않는 정보 → `const`
- ✍️ **필기** — 기억장소 할당 = 주소(address) = 번지. 컴파일 타임에 변수 이름이 기억장치 주소로 바뀐다 (예: `n` → 100번지, 메모리 주소 100 101 102 103 …)
- ✍️ **필기** — 소스(`.c`) → 컴파일(`.obj`) → 링크(`.exe`) → 실행 / 컴파일 타임 ↔ 런 타임
- 변수 선언: 저장공간 할당 / 변수 초기화: 선언하면서 초깃값 대입
```c
int n;          // 선언: 4byte 공간만 할당 (어떤 값인지 모름)
n = 10;         // 대입
n = 20;         // 변수는 값이 바뀔 수 있다

int value = 0;              // 선언 + 초기화
char name = 'a';            // 'a' 의 ASCII 코드 97 저장
int data1, data2, data3;    // 여러 변수 한 줄 선언
```
- 변수 표기법: 카멜 표기법 `myName` · 헝가리언 표기법 `c_name`
- 변수 이름 규칙: 영문자·숫자·밑줄만 사용 / 숫자로 시작 X / 공백 X / 대·소문자 구별 / 예약어(키워드) 사용 X

## 2. 자료형 크기와 서식 ★시험
| 구분 | 자료형 | 크기 | printf 서식 | 비고 |
|---|---|---|---|---|
| 문자형 | `char` | 1byte | `%c` | -128 ~ 127 (unsigned char: 0 ~ 255) |
| 정수형 | `short` | 2byte | `%d` | -32,768 ~ 32,767 |
| 정수형 | `int` | 4byte | `%d` | -2,147,483,648 ~ 2,147,483,647 |
| 정수형 | `long` | 4byte (VS) / 8byte (Mac·Linux) | `%ld` | 강의자료 표 기준 int와 같은 범위 |
| 실수형 | `float` | 4byte | `%f` | 소수점 아래 6자리 출력 |
| 실수형 | `double` | 8byte | `%lf` (printf 는 `%f` 도 가능) | scanf 로 입력받을 때는 반드시 `%lf` |

- ✍️ **필기** — `signed`(부호 있음, 기본값) ↔ `unsigned`(부호 없음) · 예: `signed int n;` / `unsigned int n;`
- ⭐ **강조** — 부호 비트: 맨 앞 1비트가 0이면 양수, 1이면 음수 → 1byte 부호 있음 -128 ~ 127 / 부호 없음 0 ~ 255
- `10000000` → 부호 있음 -128 / 부호 없음 128 · `11111111` → 부호 있음 -1 / 부호 없음 255
- ✍️ **필기 정정** — `unsigned char (1)~(256)` ❌ → **0 ~ 255** ✅ · signed char 는 -128 ~ -1 음수, 0 ~ 127 양수

## 3. 서식 지정자 (% 형식 연산자)
| 서식 | 의미 | 예시 → 결과 |
|---|---|---|
| `%d` | 10진 정수 | `printf("%d", 100);` → 100 |
| `%o` | 8진수 | `printf("%o", 100);` → 144 |
| `%x` / `%X` | 16진수 (소문자 / 대문자) | `printf("%x", 100);` → 64 |
| `%c` | 문자 1개 | `printf("%c", 65);` → A |
| `%s` | 문자열 (여러 글자) | `printf("%s", "안녕하세요");` |
| `%f` / `%lf` | 실수 (float / double) | `printf("%f", 3.14);` → 3.140000 |
| `%p` | 주소 | `printf("%p", &age);` |

- ✍️ **필기 정정** — `0진법 → %d` ❌ → **10진법** `%d` ✅
- ⭐ **강조** — `'A'` 는 문자 1개(`%c`), `"A"` 는 문자열(`%s`, 끝에 널 문자 포함) → `printf("%c", "A");` ❌ · `char a1 = "c";` ❌ → `'c'` ✅
- 출력 칸수 조정: `printf("[%5d] [%05d] [%-5d]", 7, 7, 7);` → `[    7] [00007] [7    ]`

## 4. ASCII 코드와 2진수
- ✍️ **필기** — 문자는 1byte · `'A'` = 65(10진수) = `01000001`(2진수) · `'a'` = 97

| 128 | 64 | 32 | 16 | 8 | 4 | 2 | 1 |
|---|---|---|---|---|---|---|---|
| 0 | 1 | 0 | 0 | 0 | 0 | 0 | 1 |

- 64 + 1 = **65** → `printf("%c", 65);` 는 `A` 를 출력한다

## 5. 상수: const 와 #define
- ⭐ **강조** — `const` 상수는 **선언과 동시에 초기화**해야 하고, 값을 다시 대입하면 컴파일 오류
- ⭐ **강조** — 매크로 상수 `#define`: `#` 으로 시작하는 구문은 컴파일 전에 **전처리기**가 해석·치환 (끝에 `;` 안 붙임)
```c
// ex01_04-const.c: 상수
#include <stdio.h>
int main() {
    const int a1 = 1000;    // 상수는 선언과 동시에 초기화해야
    printf("a1: %d \n", a1);
    // a1 = 2000;           // error: 상수는 재할당 불가능
    printf("a1: %d \n", a1);
    return 0;
}
```
```c
// #define 매크로 상수 + const 상수
#define DATA1 100
#include <stdio.h>
int main() {
    const int a1 = 1000;
    printf("a1 %d DATA: %d\n", a1, DATA1);    // a1 1000 DATA: 100
    return 0;
}
```

## 6. scanf() 와 & 주소 연산자
- ⭐ **강조** — `scanf("%d", &age);` : `&` 로 변수의 **시작 주소**를 넘겨야 입력값이 그 기억장소에 저장된다
- `printf("%p, %d\n", &age, age);` → 주소와 값을 출력 (예: `0x16f3eb3d8, 21`)
- VS 에서 scanf 를 쓰려면 맨 위에 `#define _CRT_SECURE_NO_WARNINGS` · 앞의 밑줄이나 철자가 틀리면 C4996 오류
```c
// Prj06/06scanf.c  printf(), scanf()
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void) {
    int month = 0;
    printf("1년은 몇 달? ");
    scanf("%d", &month);
    printf("1년은 %d달\n\n", month);

    int snum, credit;
    printf("당신의 학번과 신청 학점은? ");
    scanf("%d %d", &snum, &credit);
    printf("학번:  %d 신청학점: %d\n", snum, credit);
    return 0;
}
```

## 7. 실습 코드 (w03)
- 1-00 `ex01_00-var.c` 리터럴: 정수 `100`(`%d`) · 실수 `3.14`(`%f`) · 문자 `'A'`(`%c`) · 문자열 `"안녕하세요"`(`%s`)
- 1-01 `ex01_01-var.c` 초기화하지 않은 변수 출력 → 쓰레기값 (VS 에서는 **C4700 오류**)
- 1-02 `ex01_02-var.c` 변수 초기화 → `a1: c` · `a2: 100` · `a3: 11.110000`
- 1-03 `ex01_03-var.c` 선언과 초기화 (`int a7, a8, a9;` 한 줄 선언 → 300 · 400 · 500)
- 1-04 `ex01_04-const.c` const 상수(→ 5장) · 1-05 `ex01_05-sizeof.c` sizeof(→ 8장) · 2-01 `06scanf.c` scanf(→ 6장)
```c
// ex01_02-var.c: 변수 초기화
#include <stdio.h>
int main() {
    char  a1;
    int   a2;
    float a3;

    a1 = 'c';
    a2 = 100;
    a3 = 11.11;

    printf("a1: %c \n", a1);    // a1: c
    printf("a2: %d \n", a2);    // a2: 100
    printf("a3: %f \n", a3);    // a3: 11.110000
    return 0;
}
```

## 8. ★ 시험문제 정리
### ① 강의자료 23p — [실습 1-05] ex01_05-sizeof.c 실행결과 쓰기
```c
// ex01_05-sizeof.c
#include <stdio.h>
int main() {
    char gender;
    int age;
    float weight;
    double height;

    printf("변수의 할당크기\n");
    printf("gender: %d, age: %d, weight: %d, height: %d\n\n",
        sizeof(gender), sizeof(age), sizeof(weight), sizeof(height));

    printf("자료형 크기\n");
    printf("char: %d, int: %d, float: %d, double: %d\n\n",
        sizeof(char), sizeof(int), sizeof(float), sizeof(double));

    printf("정수형상수: %d, 실수형상수: %d\n",
        sizeof(10), sizeof(3.14159));
    return 0;
}
```
```text
변수의 할당크기
gender: 1, age: 4, weight: 4, height: 8

자료형 크기
char: 1, int: 4, float: 4, double: 8

정수형상수: 4, 실수형상수: 8
```
- ⭐ **강조** — `sizeof` 는 크기(바이트 수)를 **정수**로 돌려준다 → `%d` 로 출력
- ⭐ **강조** — `sizeof(10)` → 정수 상수는 int → **4** / `sizeof(3.14159)` → 실수 상수는 double → **8** (`3.14f` 처럼 f 를 붙이면 float → 4)
- ✍️ **필기** — (수업 중 "시험문제!") `sizeof(float)` 을 `%f`, `sizeof(double)` 을 `%lf`, `sizeof(char)` 를 `%c` 로 찍으면 ❌ → **전부 `%d`** → `int: 4` · `float: 4` · `double: 8` · `char: 1`

### ② 강의자료 27p — [quiz 1-01] 빈칸채우기 · 출력결과가 나오도록 프로그램 작성
- [출력] `성별: w, 나이: 21, 신장: 165.700000`
- ✏️ 자료에서 printf 의 서식 문자열이 가려져 있다(빈칸 ①) · 코드 안에 **오류가 있는 줄**도 하나 숨어 있다
```c
// quiz01-01_ex01.c (문제)
#include <stdio.h>
int main() {
    char gender;
    int age;
    double height;

    gender = 'w';
    age = 21;
    height = 165.7

    printf("______①______%lf\n", gender, age, height);
    return 0;
}
```
1. 빈칸 ①에 들어갈 서식 문자열은?
2. `double` 변수 `height` 를 printf 로 출력하는 서식 지정자는?
3. 오류가 있는 줄을 찾아 바르게 고쳐 쓰면?

<details>
<summary>💡 빈칸채우기 정답 보기</summary>

1. `성별: %c, 나이: %d, 신장: ` → char 는 `%c`, int 는 `%d` (뒤의 `%lf\n` 은 자료에 보이는 부분)
2. `%lf` — printf 에서는 `%f` 도 OK, scanf 로 입력받을 때는 반드시 `%lf`
3. `height = 165.7;` — 세미콜론(;)이 빠져 있으면 컴파일 오류

- ⭐ **강조** — 실수는 소수점 아래 **6자리**까지 출력 → `165.700000`
```c
// quiz01-01_ex01.c (정답)
#include <stdio.h>
int main() {
    char gender;
    int age;
    double height;

    gender = 'w';
    age = 21;
    height = 165.7;     // 오류 수정: ; 가 빠지면 컴파일 오류

    printf("성별: %c, 나이: %d, 신장: %lf\n", gender, age, height);   // 빈칸 ①
    return 0;
}
```

</details>

## 9. 오류 수정 모음 ★빈칸채우기·오류수정 대비
- `printf("문자 리터럴 %c\n", "A");` ❌ → `'A'` ✅
- `a1 = "c";` ❌ → `a1 = 'c';` ✅
- 서식표 빈칸: `short → %s` ❌ → `%d` ✅ · `long → %` ❌ → `%ld` ✅ · `0진법` ❌ → `10진법 %d` ✅
- `unsigned char (1)~(256)` ❌ → `0 ~ 255` ✅
- `#define CRT_SECUE_NO_WARNINGS` ❌ → `#define _CRT_SECURE_NO_WARNINGS` ✅
- `printf("float: %f \n", sizeof(float));` ❌ → `%d` ✅ (sizeof 결과는 정수)

<details>
<summary>✍️ 수업 중 원본 필기 (정리 전 메모)</summary>

```text
변수(variable): 기억장소 · 상수(constant): const · 기억장소 할당: 주소(address). 번지 · int n; n=10; n=20;
char a1; 1byte (-128)~(+127) · int a2; 4byte · float a3; 4byte · double a4; 8byte
signed char (-128)~(+127) · unsigned char (1)~(256) · -128~-1:음수 · 0~255:양수
자료형(data types): 부호가 있는 문자형 signed int n;(default 값) · 부호가 없는 문자형 unsigned int n;
0진법 → %d · 8진법 → %o · 16진법 → %x
문자형 char → 1바이트 → %c · 숫자형 short → 2바이트 → %d · int → 4바이트 → %d · long → 8바이트 → %ld · 실수형 float → 4바이트 → %f · double → 8바이트 → %lf
ASCII code · 문자 1byte · A 65 10진수 → 2진수 (128 64 32 16 8 4 2 1 / 0 1 0 0 0 0 0 1) · a97
소스 → 컴파일 → 링크 → 실행 (.c .obj .exe) · 컴파일 타임: 변수 → 기억장치 주소 (n → 100번지) · 컴파일 타임 / 런 타임 · 100 101 102 103 104
% 형식 연산자: %d:10진수 · %f:실수 · %c:한글자 문자 · %s:여러개 문자
```

</details>
