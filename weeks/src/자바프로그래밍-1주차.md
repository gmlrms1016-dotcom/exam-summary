# 강의소개 / 첫 프로그램

> 자료: IMG_1831(강의소개 화면) · heegeun.java

## 1. 강의 안내
- ⭐ **강조** — 교재: 명품 Java Programming(개정5판), 황기태·김효수, 생능출판사
- ⭐ **강조** — 평가: 필기평가1 30점 + 직무수행능력평가2 15점 + 직무수행능력평가3 등 (실습 중심 평가)

## 2. 첫 자바 프로그램 (heegeun.java)
- package 선언 + `import java.util.Scanner` + public class(파일명과 동일)
- ⭐ **강조** — 자바 프로그램은 `public static void main(String[] args)` 에서 실행을 시작한다
- Scanner로 키 입력, if-else 조건문, for/while 반복문 사용 실습
```java
package heegeun;
import java.util.Scanner;
public class heegeun {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("반갑습니다. 여러분~~");
        int age = sc.nextInt();
        if (age >= 19) {
            for (int i=1; i<=10; i++) { ... }
        } else {
            System.out.printf("10대 입니다.");
        }
    }
}
```
- ✍️ **필기** — 클래스 이름과 파일 이름은 반드시 같아야 하며, main() 메서드가 있어야 실행된다
